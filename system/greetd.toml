# Optional greetd + ReGreet login.
# Verified against: ArchWiki greetd, greetd config format
# Reviewed: 2026-05-11

[terminal]
vt = 1

[default_session]
command = "dbus-run-session cage -s -m last -- regreet"
user = "greeter"
