# Optional greetd + ReGreet login.
# Verified against: ArchWiki greetd, greetd config format
# Reviewed: 2026-05-11

[terminal]
vt = 1

[default_session]
command = "env LANG=es_PE.UTF-8 LC_TIME=es_PE.UTF-8 LC_ALL=es_PE.UTF-8 dbus-run-session cage -s -m last -- regreet"
user = "greeter"
