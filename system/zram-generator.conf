# Verified against: zram-generator.conf(5)
# Reviewed: 2026-06-10

[zram0]
# 16 GB machine: ram/2 (~8 GB) gives Docker-heavy sessions more headroom
# than the old 4 GB cap; zstd keeps the compressed cost reasonable.
zram-size = min(ram / 2, 8192)
compression-algorithm = zstd
swap-priority = 100
